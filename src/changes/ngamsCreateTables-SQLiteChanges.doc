Changes to ngamsCreateTables-SQLite.sql:

Modifications to ngas_files table:
Added column 'container_id' as VARCHAR(36), Null
Added column 'ingestion_rate' as int, null (should be not null once implemented fully)

Addition of ngas_containers table:
container_id as varchar(36) and not null as is primary key
container_name as varchar(255) and not null, store semantic name
ingestion_date as varchar(23) and not null, store date archived
container_siz as numeric(20,0) and not null, store size of entire container
