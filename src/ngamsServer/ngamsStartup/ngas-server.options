# Options configuration for the ngas-server script
#
# This file defines the configuration options for the NGAS server daemon
# started by the init system
#

#
# The user used to run NGAS
#
USER=ngas

#
# The name of the script that controls the server daemon
# The following options are currently available
#
#  ngamsDaemon      Starts a normal server
#  ngamsCacheDaemon Starts an NGAS server in cache mode
#
DAEMON=ngamsDaemon